{
  "name": "matrikkel-actions",
  "type": "module",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "build:slack-approval-notifier": "cd slack-approval-notifier && bun run build",
    "build": "run-p build:*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "husky": "^9.1.7",
    "npm-run-all": "^4.1.5"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.1",
    "@slack/web-api": "^7.13.0"
  }
}
