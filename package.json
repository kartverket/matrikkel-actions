{
  "name": "matrikkel-actions",
  "type": "module",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "build:slack-approval-notifier": "cd slack-approval-notifier && bun run build",
    "build:skip-deploy": "cd skip-deploy && run-p build:*",
    "build:k8s-feedback": "cd k8s-deployment-feedback && bun run build",
    "build": "run-p build:*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/yaml": "^1.9.7",
    "husky": "^9.1.7",
    "npm-run-all": "^4.1.5"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.1",
    "@nutgaard/bun-recording-shell": "^0.0.2",
    "@slack/web-api": "^7.13.0",
    "yaml": "^2.9.0",
    "zod": "^4.4.3"
  }
}
